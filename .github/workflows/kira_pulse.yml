name: Kira Pulse

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout codigo
        uses: actions/checkout@v3

      - name: Instalar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Instalar librerias
        run: pip install yfinance firebase-admin

      - name: Verificacion de archivos
        run: |
          echo "Buscando archivos en el servidor..."
          ls -R

      - name: Ejecutar motor Kira
        env:
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_CONFIG }}
        run: |
          if [ -f "data_bridge.py" ]; then
            python data_bridge.py
          elif [ -f "scripts/data_bridge.py" ]; then
            python scripts/data_bridge.py
          else
            echo "❌ ERROR CRITICO: data_bridge.py no aparece en ninguna parte."
            exit 1
          fi
